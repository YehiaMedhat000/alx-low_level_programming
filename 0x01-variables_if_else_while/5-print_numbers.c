#include <string.h>
#include <stdio.h>
/**
 * main - prints the alphabet lower case
 *
 * Return: 0 (sucess)
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
	{
		printf("%d", i);
	}

	printf("\n");
	return (0);
}
