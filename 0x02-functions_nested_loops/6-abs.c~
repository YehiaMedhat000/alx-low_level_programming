#include "main.h"
/**
 * print_sign - prints some word
 * @n: parameter of type int
 * Return: Always 0 (success)
 */

int print_sign(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-1 * n);
	}
}

